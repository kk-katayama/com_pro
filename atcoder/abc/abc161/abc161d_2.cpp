#include <iostream>
#include <algorithm>
#include <vector>
#define rep(i,n) for(int i=0;i<n;++i)
#define rep1(i,n) for(int i=1;i<=n;++i)
using namespace std;
typedef long long ll;
vector<ll> res;
void dfs(ll x, int i) {
  if(i == 9) return;
  res.push_back(x);
  if(x % 10 == 0) {
    
  }
}
int main()
{

  return 0;
}
