#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#define rep(i,n) for(int i=0;i<n;++i)
#define rep1(i,n) for(int i=1;i<=n;++i)
using namespace std;
template<class T>bool chmax(T &a, const T &b) { if(a < b){ a = b; return 1; } return 0; }
template<class T>bool chmin(T &a, const T &b) { if(a > b){ a = b; return 1; } return 0; }
typedef long long ll;
int main()
{
  int n;cin >> n;
  int m = 3*n;
  vector<ll> a(m);
  rep(i,m) cin >> a[i];

  priority_queue<ll, vector<ll>, greater<ll>> fro, mid, bac;
  rep(i,n) {
    fro.push(a[i]);
  }

  vector<ll> b(2*n);
  rep(i,
  
  return 0;
}
